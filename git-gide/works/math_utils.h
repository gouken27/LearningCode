// math_utils.h — утилиты для математических операций
// Свободные функции (не метод класса) — тоже хороший стиль.

#pragma once

// Умножает два целых числа и возвращает результат
int multiply(int a, int b);

// Возвращает факториал числа n (n >= 0)
long long factorial(int n);
